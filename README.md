# LABITCONF 2026 — update 2026-09-23 (c)

Contenido:

- `app/index.html` — HTML publicado del artefacto "Speakers LABITCONF 2026" (v135).
- `backup-db/` — copia de la base viva del artefacto: `agenda/`, `grid/`, `config/`, `appsync/`, `extras/`,
  más `base-speakers-embebida.json` (el `var D` del HTML) y `grilla-legible.md`.

Cambio de esta versión: el filtro `REVISAR` de la lista de Speakers pasa a llamarse
**"◐ Falta profundizar"** (antes "◐ Revisé, falta profundizar"), igual que ya se llamaba en
la solapa de Charlas destacadas. El filtro en sí y la marca ◐ ya existían; solo cambió la etiqueta.

Antes en el mismo día: v133 sumó un filtro de MAIL y v134 lo revirtió.

Repo privado: lleva mails, teléfonos y notas internas de speakers.
